import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "react";
type LoaderIconProps = {
    size?: number;
};
const {{variable-name}} = ({ size = 16 }: LoaderIconProps) => {
    console.warn("TODO: Implement LoaderIcon");
    return undefined;
};
export type LoaderProps = HTMLAttributes<HTMLDivElement> & {
    size?: number;
};
export const {{variable-name}} = ({ className, size = 16, ...props }: LoaderProps) => {
    console.warn("TODO: Implement Loader");
    return undefined;
};
