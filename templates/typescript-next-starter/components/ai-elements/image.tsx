import { cn } from "@/lib/utils";
import type { Experimental_GeneratedImage } from "ai";
export type ImageProps = Experimental_GeneratedImage & {
    className?: string;
    alt?: string;
};
export const {{variable-name}} = ({ base64, uint8Array, mediaType, ...props }: ImageProps) => {
    console.warn("TODO: Implement Image");
    return undefined;
};
